name: Publish

on:
  push:
    tags:
      - "v*"

jobs:
  gate:
    # Blocking pre-publish eval gate; secrets: inherit forwards OPENAI_API_KEY.
    uses: ./.github/workflows/publish-gate.yml
    secrets: inherit

  publish-test:
    needs: gate
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write  # Required for trusted publishing

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.13

      - name: Build package
        run: uv build

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  publish:
    needs: publish-test
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # Required for trusted publishing

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.13

      - name: Build package
        run: uv build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  publish-mcp-registry:
    # MCP Registry publish via GitHub OIDC (io.github.devemberx/* namespace, no
    # secret). Registry validates the PyPI package, so wait-for-index runs first.
    # Not idempotent: re-running a tag whose version is already in the registry
    # fails at the publish step. Recovery is a new version tag, not a re-run.
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # GitHub OIDC for the io.github.* namespace
      contents: read

    steps:
      - uses: actions/checkout@v6

      - name: Wait for PyPI to index the new version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          for i in $(seq 1 20); do
            curl -fsSL "https://pypi.org/pypi/mcp-server-polarion/${VERSION}/json" >/dev/null 2>&1 \
              && { echo "PyPI has ${VERSION}"; exit 0; }
            echo "waiting for PyPI ${VERSION} ($i)"; sleep 15
          done
          echo "PyPI did not index ${VERSION}"; exit 1

      - name: Inject release version into server.json
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          jq --arg v "$VERSION" '.version = $v | (.packages[].version) = $v' \
            server.json > server.tmp && mv server.tmp server.json

      - name: Install mcp-publisher
        # Pinned to a release + sha256: this job holds an OIDC token, so a
        # compromised "latest" asset must not run here. Runner is always
        # linux/amd64; bump version + checksum together when upgrading.
        env:
          MCP_PUBLISHER_VERSION: v1.7.9
          MCP_PUBLISHER_SHA256: ab128162b0616090b47cf245afe0a23f3ef08936fdce19074f5ba0a4469281ac
        run: |
          curl -fsSL -o mcp-publisher.tar.gz \
            "https://github.com/modelcontextprotocol/registry/releases/download/${MCP_PUBLISHER_VERSION}/mcp-publisher_linux_amd64.tar.gz"
          echo "${MCP_PUBLISHER_SHA256}  mcp-publisher.tar.gz" | sha256sum -c -
          tar xz -f mcp-publisher.tar.gz mcp-publisher

      - name: Authenticate to MCP Registry (GitHub OIDC)
        run: ./mcp-publisher login github-oidc

      - name: Publish to MCP Registry
        run: ./mcp-publisher publish

  release:
    # GitHub Release; needs publish (not the registry) so a flaky preview
    # registry can't block notes. Notes built from merged PRs via release.yml.
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required to create the release

    steps:
      - uses: actions/checkout@v6

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # idempotent on re-run
          gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1 && exit 0
          python3 .github/scripts/build_release_notes.py > "${RUNNER_TEMP}/notes.md"
          gh release create "${GITHUB_REF_NAME}" \
            --verify-tag \
            --title "${GITHUB_REF_NAME}" \
            --notes-file "${RUNNER_TEMP}/notes.md"
